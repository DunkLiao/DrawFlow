# DrawFlow - 里程碑時間線 Excel 生成系統

一個功能完整的 Python 專案，用於讀取 Excel 檔案中的里程碑數據，自動生成高質量的視覺化時間線 Excel 報告。支持大規模數據集、自動分頁、統計分析和企業級排版。

## ✨ 核心特性

### 視覺化與排版

- 🎨 **彩色水平時間線**: 採用藍-綠-紅漸變色彩，視覺層次清晰
- 📐 **自適應版面設計**: 根據日期跨度自動調整時間線寬度，確保最佳可讀性
- 📄 **智能分頁系統**: 默認每頁 50 個里程碑，超出自動分頁，保持佈局整潔
- 🎯 **企業級排版**: A4 頁面、20mm 邊距、標準字體層級

### 數據處理

- 🔄 **自動去重與驗證**: 移除重複數據及無效日期記錄
- 📅 **多格式日期支持**: 自動識別 YYYY-MM-DD、YYYY/MM/DD、MM/DD/YYYY 等多種日期格式
- 🔗 **同日期事件合併**: 相同日期的多個事件自動組合顯示，用逗號分隔
- 📊 **智能排序**: 按日期遞增排序，確保時間線邏輯清晰

### 統計分析

- 📈 **綜合統計面板**: 包含以下關鍵指標
  - 里程碑總數
  - 時間跨度（開始日期 → 結束日期）
  - 總天數和總月數
  - 里程碑密度（月均里程碑數）
  - 月度分佈柱狀圖
- 📉 **月度分佈分析**: 視覺化展示各月里程碑數量分佈

### 技術特性

- 📝 **完整日誌記錄**: 詳細的執行日誌保存到 `milestone_timeline.log`，便於調試和監控
- 🔧 **高度可配置**: 通過 `config/settings.py` 自定義色彩、字體、分頁等參數
- 💾 **高解析度輸出**: 支持 300 DPI 高質量 Excel 報告嵌入
- 🌐 **中文支持**: 完整的繁體中文字體配置，支持 Windows、macOS、Linux

## 📁 項目結構

```
DrawFlow/
├── main.py                      # 主程序入口 - 完整工作流協調
├── requirements.txt             # Python 依賴包
├── README.md                    # 本文件
├── milestone_timeline.log       # 運行日誌（自動生成）
├──
├── config/                      # 配置模塊
│   ├── __init__.py
│   └── settings.py             # 色彩方案、排版參數、常數定義
│
├── src/                         # 核心模塊
│   ├── __init__.py
│   ├── excel_reader.py         # Excel 讀取和數據驗證
│   ├── data_processor.py       # 數據處理、統計分析、分頁邏輯
│   ├── visualizer.py           # 圖表生成、時間線繪製、統計面板
│   ├── excel_generator.py      # Excel 檔案生成、圖片嵌入
│   └── pdf_generator.py        # （備選）PDF 直接生成方案
│
├── data/                        # 數據目錄
│   ├── input/                  # 輸入 Excel 檔案目錄
│   └── output/                 # 生成的 Excel 報告目錄
│
└── __pycache__/                # Python 緩存目錄

```

## 🚀 快速開始

### 1. 安裝依賴

```bash
# 建立虛擬環境（推薦）
python -m venv venv
venv\Scripts\activate  # Windows
source venv/bin/activate  # macOS/Linux

# 安裝依賴包
pip install -r requirements.txt
```

**依賴包**:

- `pandas>=2.0.0` - 數據處理和 Excel 讀寫
- `openpyxl>=3.1.0` - Excel 文件格式支持
- `matplotlib>=3.7.0` - 圖表生成和繪製
- `pillow>=9.0.0` - 圖片處理和格式轉換
- `numpy>=1.23.0` - 數值計算和數組操作
- `python-dateutil>=2.8.0` - 靈活的日期解析

### 2. 準備輸入數據

在 `data/input/` 目錄下放置 Excel 檔案，格式要求：

| 列  | 名稱 | 說明             | 示例                               |
| --- | ---- | ---------------- | ---------------------------------- |
| 1   | 日期 | 支持多種日期格式 | 2026-01-15, 2026/01/15, 01/15/2026 |
| 2   | 事件 | 里程碑事件描述   | 需求評審、開發完成                 |

**示例數據**:

```
日期         事件
2026-01-15   需求評審
2026-02-20   開發完成
2026-03-10   測試上線
2026-03-10   質量驗收
2026-04-05   上線發布
```

### 3. 運行程序

**最簡單的方式**：

```bash
python main.py
```

程序會自動：

1. 讀取 `data/input/sample_milestones.xlsx`（如果存在）
2. 進行數據驗證和處理
3. 生成統計信息
4. 創建視覺化圖表
5. 保存為 `data/output/timeline_report.xlsx`

**指定自定義檔案**：

```python
from main import main

output_path = main(
    input_excel_path='data/input/project_milestones.xlsx',
    output_excel_path='data/output/custom_report.xlsx',
    title='2026 年項目計劃'
)
print(f"✓ 報告已生成: {output_path}")
```

### 4. 查看結果

生成的 Excel 報告位於 `data/output/timeline_report.xlsx`，包含：

- **標題區**: 報告名稱和視覺化時間線
- **統計面板**: 關鍵指標和月度分佈圖表
- **時間線圖表**: 彩色里程碑、事件標籤、日期標記、分頁符

## ⚙️ 配置說明

編輯 `config/settings.py` 自定義以下參數：

### 排版參數

```python
MARGIN_MM = 20              # 頁邊距（毫米）
TITLE_FONT_SIZE = 20        # 標題字體大小（點）
STAT_FONT_SIZE = 14         # 統計面板字體大小
LABEL_FONT_SIZE = 12        # 里程碑標籤字體大小
SMALL_FONT_SIZE = 10        # 小字體大小

PAGE_WIDTH = 8.27           # A4 頁寬（英寸）
PAGE_HEIGHT = 11.69         # A4 頁高（英寸）

MONTHLY_CHART_WIDTH_CM = 8  # 月度分佈圖寬度（厘米）
MONTHLY_CHART_HEIGHT_CM = 3 # 月度分佈圖高度（厘米）
```

### 色彩方案

```python
# 時間線漸變色（從早期到後期）
COLOR_GRADIENT_START = (0.2, 0.6, 1.0)  # 藍色
COLOR_GRADIENT_MID = (0.2, 1.0, 0.2)    # 綠色
COLOR_GRADIENT_END = (1.0, 0.2, 0.2)    # 紅色

# 統計面板
STAT_PANEL_COLOR = (1.0, 0.95, 0.8)     # 金黃色背景
STAT_PANEL_EDGE_COLOR = (0.8, 0.7, 0.3) # 棕色邊框

# 其他元素
TIMELINE_AXIS_COLOR = (0.3, 0.3, 0.3)   # 深灰色軸線
TEXT_COLOR = (0.2, 0.2, 0.2)             # 深灰色文字
```

### 分頁設定

```python
MILESTONES_PER_PAGE = 50    # 每頁最大里程碑數
DPI = 300                    # 輸出解析度（DPI）
```

### 支持的日期格式

```python
DATE_FORMATS = [
    '%Y-%m-%d',    # 2026-01-15
    '%Y/%m/%d',    # 2026/01/15
    '%d-%m-%Y',    # 15-01-2026
    '%d/%m/%Y',    # 15/01/2026
    '%m-%d-%Y',    # 01-15-2026
    '%m/%d/%Y',    # 01/15/2026
]
```

## 📚 API 文檔

### ExcelReader（Excel 讀取器）

```python
from src.excel_reader import ExcelReader

# 初始化讀取器
reader = ExcelReader('data/input/milestones.xlsx')

# 讀取里程碑數據
df = reader.read_milestone_data(
    sheet_name=0,      # 工作表索引或名稱
    date_col=0,        # 日期列索引
    event_col=1        # 事件列索引
)
```

**返回值**: pandas DataFrame，包含 'date' 和 'event' 列

- 自動轉換日期格式
- 移除無效日期記錄
- 移除空行和重複項

### DataProcessor（數據處理器）

```python
from src.data_processor import DataProcessor

# 初始化處理器
processor = DataProcessor(milestones_per_page=50)

# 完整處理流程
merged_df, stats, pages = processor.process_all(df)
```

**主要方法**:

- `process_data(df)` - 初步驗證和去重
- `merge_same_date_events(df)` - 合併同日期事件
- `calculate_statistics(df)` - 計算統計信息
- `paginate_data(df)` - 按指定數量分頁

**stats 字典內容**:

```python
{
    'total_milestones': 45,              # 里程碑總數
    'start_date': Timestamp('2026-01-15'),
    'end_date': Timestamp('2026-04-05'),
    'total_days': 81,                    # 日期跨度
    'total_months': 4,                   # 月份數
    'milestone_density': 11.25,          # 月均里程碑數
    'monthly_distribution': [            # 月度分佈
        {'period': '2026-01', 'count': 8},
        {'period': '2026-02', 'count': 12},
        # ...
    ]
}
```

### TimelineVisualizer（可視化生成器）

```python
from src.visualizer import TimelineVisualizer

# 初始化可視化器（使用默認配置）
visualizer = TimelineVisualizer()

# 或使用自定義配置
visualizer = TimelineVisualizer(config=my_config)

# 生成圖表
figures = visualizer.generate_pdf(
    pages,        # 分頁數據列表
    stats,        # 統計信息字典
    title="項目時間線"  # 報告標題
)
```

**返回值**:

- matplotlib Figure 物件列表，每個 Figure 對應一頁

### ExcelGenerator（Excel 生成器）

```python
from src.excel_generator import ExcelGenerator

# 初始化生成器
excel_gen = ExcelGenerator(dpi=300)

# 保存為 Excel 檔案
output_path = excel_gen.save_excel(
    figures,                              # 圖表列表
    'data/output/timeline_report.xlsx',   # 輸出路徑
    stats,                                # 統計信息
    title='項目時間線'                    # 報告標題
)
```

**功能**:

- 將圖表轉換為高質量圖片
- 嵌入統計信息表格
- 自動創建工作簿和工作表
- 清理臨時文件

## 💡 使用示例

### 示例 1：最簡單的用法

```bash
# 直接運行
python main.py

# 將自動尋找 data/input/sample_milestones.xlsx 並生成報告
```

### 示例 2：完整的編程流程

```python
from main import main

# 生成報告，所有參數都有默認值
output_path = main(
    input_excel_path='data/input/my_project.xlsx',
    output_excel_path='data/output/my_report.xlsx',
    title='My Project Timeline'
)

print(f"✓ 報告已生成: {output_path}")
```

### 示例 3：分步驟定制處理

```python
from src.excel_reader import ExcelReader
from src.data_processor import DataProcessor
from src.visualizer import TimelineVisualizer
from src.excel_generator import ExcelGenerator

# 步驟 1: 讀取 Excel
reader = ExcelReader('data/input/milestones.xlsx')
df = reader.read_milestone_data()
print(f"讀取 {len(df)} 條記錄")

# 步驟 2: 數據處理和統計
processor = DataProcessor(milestones_per_page=50)
merged_df, stats, pages = processor.process_all(df)
print(f"合併為 {len(merged_df)} 個里程碑")
print(f"時間跨度: {stats['start_date']} 至 {stats['end_date']}")

# 步驟 3: 生成可視化圖表
visualizer = TimelineVisualizer()
figures = visualizer.generate_pdf(pages, stats, title="項目計劃")
print(f"生成 {len(figures)} 頁圖表")

# 步驟 4: 保存為 Excel
excel_gen = ExcelGenerator(dpi=300)
output_path = excel_gen.save_excel(
    figures,
    'data/output/timeline.xlsx',
    stats,
    title='項目計劃'
)
print(f"✓ 報告已保存: {output_path}")
```

### 示例 4：批量處理多個檔案

```python
from pathlib import Path
from main import main

input_dir = Path('data/input')
output_dir = Path('data/output')

# 處理目錄中的所有 Excel 檔案
for excel_file in input_dir.glob('*.xlsx'):
    output_file = output_dir / f"{excel_file.stem}_report.xlsx"

    try:
        output_path = main(
            input_excel_path=str(excel_file),
            output_excel_path=str(output_file),
            title=f"{excel_file.stem} 時間線"
        )
        print(f"✓ {excel_file.name} 處理完成")
    except Exception as e:
        print(f"✗ {excel_file.name} 處理失敗: {e}")
```

## 🔍 日誌與調試

### 日誌輸出

程序執行過程中會輸出詳細日誌：

- 控制台實時輸出
- 文件日誌保存到 `milestone_timeline.log`

### 日誌示例

```
2026-01-29 10:30:45,123 - __main__ - INFO - ==================================================
2026-01-29 10:30:45,124 - __main__ - INFO - 開始生成里程碑時間線 Excel 報告
2026-01-29 10:30:45,125 - __main__ - INFO - ==================================================
2026-01-29 10:30:45,234 - __main__ - INFO - 步驟 1: 讀取 Excel 檔案: data/input/sample_milestones.xlsx
2026-01-29 10:30:45,456 - src.excel_reader - INFO - 讀取 Excel 成功，共 45 行
2026-01-29 10:30:45,789 - src.data_processor - INFO - 統計完成: 45 個里程碑，跨度 81 天，密度 11.25/月
2026-01-29 10:30:46,123 - __main__ - INFO - ✓ 報告生成完成!
```

### 故障排除

| 問題                            | 解決方案                                            |
| ------------------------------- | --------------------------------------------------- |
| `FileNotFoundError: 檔案不存在` | 確保 Excel 檔案位於 `data/input/` 目錄              |
| 日期識別失敗                    | 檢查日期格式是否在支持列表中，或手動在 Excel 中轉換 |
| 中文顯示亂碼                    | 系統缺少中文字體，自動降級到備用字體                |
| 內存不足                        | 減小 `MILESTONES_PER_PAGE` 或分批處理檔案           |
| Excel 文件過大                  | 降低 `DPI` 參數或減少里程碑數量                     |

## 📊 示例數據

### 輸入 Excel 格式

```
日期          事件
2026-01-15   需求評審 ✓
2026-01-20   需求確認
2026-02-01   架構設計
2026-02-10   技術方案評審
2026-02-20   開發開始
2026-03-05   測試環境準備
2026-03-15   測試開始
2026-03-20   bug 修複
2026-03-25   用戶驗收
2026-03-30   上線準備
2026-04-05   正式上線
```

### 輸出統計信息

```
里程碑總數: 11
開始日期: 2026-01-15
結束日期: 2026-04-05
時間跨度: 81 天
里程碑密度: 2.75 個/月
月度分佈:
  - 2026-01: 2 個
  - 2026-02: 4 個
  - 2026-03: 4 個
  - 2026-04: 1 個
```

## ⚡ 性能指標

| 指標         | 數值                     |
| ------------ | ------------------------ |
| 處理速度     | ~100-500 里程碑/秒       |
| 內存占用     | ~50-100 MB（典型場景）   |
| 最大里程碑數 | 無實際限制（理論上）     |
| 輸出文件大小 | ~5-20 MB（100 個里程碑） |
| 分頁粒度     | 可配置（默認 50 個/頁）  |

## 🔧 高級用法

### 自定義色彩方案

編輯 `config/settings.py`：

```python
# 示例：紫-青-黃漸變
COLOR_GRADIENT_START = (0.5, 0.0, 1.0)  # 紫色
COLOR_GRADIENT_MID = (0.0, 1.0, 1.0)    # 青色
COLOR_GRADIENT_END = (1.0, 1.0, 0.0)    # 黃色
```

### 自定義分頁數

```python
processor = DataProcessor(milestones_per_page=100)  # 每頁 100 個
```

### 自定義字體

在 `src/visualizer.py` 的 `setup_chinese_fonts()` 函數中修改字體列表

## 📝 FAQ（常見問題）

**Q: 如何處理日期格式錯誤？**  
A: 程序自動支持多種日期格式。如果仍有問題，建議在 Excel 中確認日期列的格式為「日期」類型。

**Q: 同一日期有多個事件如何顯示？**  
A: 自動合併為一行，事件間用逗號分隔，例如「需求評審, 需求確認」。

**Q: 支持多少個里程碑？**  
A: 理論上無限制。超過 50 個自動分頁，每頁最多 50 個（可配置）。

**Q: 能否修改時間線的顏色？**  
A: 可以。編輯 `config/settings.py` 中的 `COLOR_GRADIENT_*` 參數。

**Q: 輸出格式只支持 Excel 嗎？**  
A: 當前主要輸出格式是 Excel。項目中包含 `pdf_generator.py` 和 `pdf_generator_reportlab.py` 作為 PDF 支持的備選方案。

**Q: 如何在報告中添加自定義信息？**  
A: 修改 `src/excel_generator.py` 中的 `save_excel` 方法，在 Excel 中添加自定義行或單元格。

**Q: 支持哪些操作系統？**  
A: Windows、macOS、Linux（已測試）。字體配置會自動適配各系統。

## 📄 許可證

MIT License

## 👨‍💻 開發資訊

**開發時間**: 2026 年 1 月  
**Python 版本**: 3.8+  
**主要技術棧**:

- pandas: 數據處理
- matplotlib: 圖表生成
- openpyxl: Excel 文件操作
- Pillow: 圖像處理

## 🤝 貢獻指南

歡迎提交 Issue 和 Pull Request 來改進本項目。

## 📞 技術支持

如遇到問題，請：

1. 查看 `milestone_timeline.log` 獲取詳細錯誤信息
2. 檢查輸入 Excel 數據格式
3. 確保所有依賴包已正確安裝
